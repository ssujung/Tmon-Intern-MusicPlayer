<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_tile_view"
        android:icon="@drawable/tileview_icon"
        android:title="@string/tile_view"
        app:showAsAction="always" />
    <item
        android:id="@+id/action_list_view"
        android:icon="@mipmap/listview_icon"
        android:title="@string/list_view"
        app:showAsAction="always" />
    <item
        android:id="@+id/action_search"
        android:icon="@mipmap/search_icon"
        android:title="@string/action_search"
        app:showAsAction="always|collapseActionView"
        app:actionViewClass="android.support.v7.widget.SearchView" />

    <item
        android:id="@+id/action_done"
        android:icon="@mipmap/search_icon"
        android:title="@string/action_done"
        app:showAsAction="always" />
</menu>